{
  "name": "msagent",
  "displayName": "msAgent",
  "description": "Agentic auto-fix for mssanitizer Ascend NPU operator memory errors",
  "version": "0.2.0",
  "publisher": "msagent",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Debuggers",
    "Testing"
  ],
  "activationEvents": [
    "onLanguage:cpp",
    "onCommand:msagent.parseLog",
    "onCommand:msagent.fixProblem",
    "onCommand:msagent.fixAll",
    "onCommand:msagent.testWebview"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "msagent.parseLog",
        "title": "msAgent: Parse Log File",
        "icon": "$(file-text)"
      },
      {
        "command": "msagent.fixProblem",
        "title": "msAgent: Fix Problem by Index"
      },
      {
        "command": "msagent.fixAll",
        "title": "msAgent: Fix All Issues",
        "icon": "$(beaker)"
      },
      {
        "command": "msagent.clearDiagnostics",
        "title": "msAgent: Clear Diagnostics",
        "icon": "$(clear-all)"
      },
      {
        "command": "msagent.openSettings",
        "title": "msAgent: Open Settings",
        "icon": "$(gear)"
      },
      {
        "command": "msagent.testWebview",
        "title": "msAgent: Test WebView",
        "icon": "$(check)"
      }
    ],
    "configuration": {
      "title": "msAgent",
      "properties": {
        "msagent.modelEndpoint": {
          "type": "string",
          "default": "http://localhost:11434",
          "description": "LLM API endpoint URL (OpenAI-compatible, e.g. Ollama, vLLM, Volcano Engine)"
        },
        "msagent.modelName": {
          "type": "string",
          "default": "qwen3:8b",
          "description": "Model name (e.g. qwen3:8b for Ollama, ep-xxx for Volcano Engine)"
        },
        "msagent.apiKey": {
          "type": "string",
          "default": "",
          "description": "API key for cloud providers (e.g. Volcano Engine). Leave empty for local providers like Ollama."
        },
        "msagent.temperature": {
          "type": "number",
          "default": 0.1,
          "minimum": 0,
          "maximum": 2,
          "description": "LLM temperature for fix generation"
        },
        "msagent.maxTokens": {
          "type": "number",
          "default": 4096,
          "minimum": 256,
          "maximum": 32768,
          "description": "Max tokens for LLM response"
        },
        "msagent.timeoutMs": {
          "type": "number",
          "default": 300000,
          "minimum": 30000,
          "maximum": 600000,
          "description": "LLM request timeout in milliseconds (default: 5 minutes for complex fixes)"
        },
        "msagent.provider": {
          "type": "string",
          "enum": [
            "openai-compatible",
            "opencode"
          ],
          "default": "openai-compatible",
          "description": "Underlying agent provider to use (openai-compatible or opencode)"
        },
        "msagent.opencodeMode": {
          "type": "string",
          "enum": [
            "cli",
            "api",
            "serve"
          ],
          "default": "serve",
          "description": "OpenCode connection mode: cli for direct CLI calls, api for remote API, serve for local HTTP server (recommended)"
        },
        "msagent.opencodeServePort": {
          "type": "number",
          "default": 4096,
          "minimum": 1024,
          "maximum": 65535,
          "description": "Port for OpenCode serve mode (default: 4096)"
        },
        "msagent.opencodeCliPath": {
          "type": "string",
          "default": "opencode",
          "description": "Path to OpenCode CLI executable (only for cli mode)"
        },
        "msagent.opencodeApiEndpoint": {
          "type": "string",
          "default": "https://api.opencode.com",
          "description": "OpenCode API endpoint URL (only for api mode)"
        },
        "msagent.opencodeApiKey": {
          "type": "string",
          "default": "",
          "description": "OpenCode API key (only for api mode)"
        }
      }
    },
    "keybindings": [
      {
        "command": "msagent.parseLog",
        "key": "Ctrl+Alt+L",
        "mac": "Cmd+Alt+L",
        "when": "editorTextFocus"
      },
      {
        "command": "msagent.fixAll",
        "key": "Ctrl+Alt+F",
        "mac": "Cmd+Alt+F",
        "when": "editorTextFocus"
      },
      {
        "command": "msagent.clearDiagnostics",
        "key": "Ctrl+Alt+C",
        "mac": "Cmd+Alt+C"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "test": "mocha",
    "test:coverage": "c8 mocha"
  },
  "devDependencies": {
    "@types/chai": "^5.2.3",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "@types/sinon": "^21.0.1",
    "@types/vscode": "^1.85.0",
    "@vscode/test-electron": "^2.3.0",
    "c8": "^11.0.0",
    "chai": "^6.2.2",
    "mocha": "^11.7.5",
    "sinon": "^21.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "claude-code": "^1.0.0"
  }
}
